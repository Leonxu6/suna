{
  "name": "@kortix/starter",
  "version": "1.0.0",
  "description": "Kortix workspace starter — folder-based template and loader. Used by the API create-repo path and the `kortix init` command.",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "typescript": "^5.0.0"
  }
}
